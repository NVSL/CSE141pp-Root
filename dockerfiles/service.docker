ARG DOCKER_RUNNER_IMAGE
FROM $DOCKER_RUNNER_IMAGE
USER root
WORKDIR /cse142L

ENV GOOGLE_APPLICATION_CREDENTIALS ./CSE141pp-Config/secrets/$GOOGLE_CREDENTIALS_FILE

ARG ARG_THIS_DOCKER_IMAGE
ENV THIS_DOCKER_IMAGE ${ARG_THIS_DOCKER_IMAGE}
ARG ARG_THIS_DOCKER_IMAGE_UUID
ENV THIS_DOCKER_IMAGE_UUID $ARG_THIS_DOCKER_IMAGE_UUID
